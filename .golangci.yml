version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
    - gocritic
    - gosec
    - errorlint
    - prealloc
    - unparam
    - nilerr
    - forcetypeassert
    - makezero
    - goconst
    - misspell
  settings:
    revive:
      rules:
        - name: error-return
        - name: error-strings
        - name: error-naming
        - name: exported
        - name: if-return
        - name: var-naming
    goconst:
      min-len: 3
      min-occurrences: 3
    gosec:
      excludes:
        - G706 # Log injection — expected for a debugging proxy that logs HTTP traffic
        # Parsing untrusted XML is the point of the highlighter. encoding/xml
        # never resolves external entities and ignores DTDs, and the input is
        # already capped at maxLogBodySize before it reaches the decoder.
        - G709
  exclusions:
    rules:
      - path: '(.+)_test\.go'
        linters:
          - gosec
          - goconst # repeated literals in table-driven test cases are intentional
          - unparam # test doubles have signatures fixed by the interface they implement
